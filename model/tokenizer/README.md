Tokenizer files (if separate) should go here. Example:
- tokenizer.json
- special_tokens_map.json
- vocab.txt

If you use Hugging Face tokenizers keep them under `model/tokenizer/` or point `SLMModel` to the tokenizer path.
